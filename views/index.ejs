<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
      }

      /* Dark Theme Background with Animated Gradient */
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background: linear-gradient(45deg, #0f0f0f, #1a1a1a, #262626);
        background-size: 400% 400%;
        animation: gradientBG 8s ease infinite;
        color: #fff;
      }

      @keyframes gradientBG {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      /* Main Container */
      .container {
        background: rgba(30, 30, 30, 0.9);
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0px 4px 15px rgba(0, 255, 170, 0.4);
        text-align: center;
        max-width: 400px;
        transform: scale(0.9);
        animation: popUp 0.8s ease-in-out forwards;
      }

      @keyframes popUp {
        from { transform: scale(0.7); opacity: 0; }
        to { transform: scale(1); opacity: 1; }
      }

      /* Heading with Neon Glow */
      h3 {
        font-size: 28px;
        margin-bottom: 20px;
        text-shadow: 0px 0px 10px #0ff;
        animation: fadeIn 1s ease-in-out;
      }

      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      /* Input Fields */
      input {
        width: 100%;
        padding: 10px;
        margin: 10px 0;
        border: none;
        border-radius: 5px;
        background: #121212;
        color: #0ff;
        outline: none;
        box-shadow: 0px 0px 10px rgba(0, 255, 170, 0.3);
      }

      /* Button with Animation */
      button {
        padding: 10px 15px;
        border: none;
        border-radius: 5px;
        background: #0ff;
        color: #000;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0px 0px 10px rgba(0, 255, 170, 0.5);
      }

      button:hover {
        background: #00e5ff;
        transform: scale(1.05);
        box-shadow: 0px 0px 15px rgba(0, 255, 170, 0.7);
      }

      /* Short URL Section */
      .short-url {
        margin-top: 20px;
        padding: 10px;
        background: rgba(0, 255, 170, 0.1);
        border-radius: 8px;
        box-shadow: 0px 0px 10px rgba(0, 255, 170, 0.3);
      }

      .short-url a {
        color: #0ff;
        font-weight: bold;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .short-url a:hover {
        color: #00e5ff;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h3>⚡ URL SHORTENER ⚡</h3>
      <form action="/short" method="POST">
        <label for="longUrl">Enter Long URL:</label>
        <input type="text" placeholder="Paste your long URL here..." name="longUrl" required />
        <button type="submit">Generate Short URL</button>
      </form>

      <% if(shortUrl) { %>
      <div class="short-url">
        <p>🔗 Short URL:</p>
        <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
      </div>
      <% } %>
    </div>
  </body>
</html>
